pipeline {
    agent any
    stages{
        stage('listar directio actual'){
            steps {
                sh 'ls -la'
            }
        }
        stage('Build de docker'){
            steps {
                sh 'docker build -t web:latest .'
            }
        }
    }
}